### 05-mapper-map-constructs

Tests if the map-type is correctly assigned when using a custom mapper and a target map directive

Use case: When creating temporary fields in MGLET, we want to have the possibility to just allocate the large data array while still copying some field metadata.
