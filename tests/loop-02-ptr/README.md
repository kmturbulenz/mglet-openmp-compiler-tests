### loop-02-ptr

Tests if a typical MGLET field can be looped in a target region using a grid loop and an inner loop over the cells.

This test constructs a pointer to cell values on a specific grid specified by its index.
This way of indexing into grids is best practice in MGLET.
