### loop-04-multiple-parallel

Testst if the compiler accept multiple parallel regions inside one omp
target teams loop region.

This testcase is only relevant for those compilers that need an explicit
parallel region to perform the correct parallelization.
