### 05-mapper-variants

Tests if different custom mappers can be used to map selective member variables.

Use case: Only map a subset of variables in MGLET's field type. E.g. only the cell content, only the buffers or only metadata.
